import { DisplaySelectedValueDirective } from './display-selected-value.directive';

describe('DisplaySelectedValueDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplaySelectedValueDirective();
    expect(directive).toBeTruthy();
  });
});
